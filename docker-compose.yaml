version: "3.8"
services:
  mysql:
    image: flask-db
    container-name: flask-db
    build: ./db
  flask-app:
    image: flask-app
    container-name: flask-app
    environment:
      DATABASE_URI: "mysql+pymysql://root:password@mysql:3306/flask-db"
    build: ./flask-app
    ports:
    - target: 5000
      published: 5000
      protocol: tcp
    depends_on:
      - mysql
